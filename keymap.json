[
	{
		"bindings": {
			"ctrl-o": "workspace::OpenFiles",  // Open files
			"ctrl-shift-o": "workspace::Open", // Open folder
			"ctrl-'": "terminal_panel::Toggle" // Toggle terminal
		}
	},
	{
		"context": "Workspace",
		"bindings": {
			"ctrl-`": null // Disable toggle terminal for this shortcut
		}
	},
	{
		"context": "Editor",
		"bindings": {
			"ctrl-m": "language_selector::Toggle" // Toggle language selector
		}
	}
]
